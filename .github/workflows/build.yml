name: Build JWS3399 RK3399 OpenWrt

# 这里必须至少定义一个触发方式，解决 "No event triggers" 错误
on:
  workflow_dispatch:  # 手动触发（你需要的方式）
  push:
    branches:
      - openwrt-23.05  # 代码提交时自动触发（可选，防止手动触发失效）

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 360
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout code
        uses: actions/checkout@v4.2.2
        with:
          ref: openwrt-23.05
          fetch-depth: 1
          submodules: 'false'

      - name: Fix permission
        run: sudo chown -R $USER:$USER .

      - name: Install dependencies
        run: |
          sudo apt update -y
          sudo apt install -y build-essential clang flex gawk gcc-multilib g++-multilib bzip2 python3 libssl-dev git wget curl unzip

      - name: Update feeds
        run: |
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Config JWS3399
        run: |
          cat > .config << EOF
          CONFIG_TARGET_rockchip=y
          CONFIG_TARGET_rockchip_armv8=y
          CONFIG_TARGET_MULTI_PROFILE=y
          CONFIG_DEVEL=y
          CONFIG_BUILD_PATENTED=y

          # 硬件驱动
          CONFIG_PACKAGE_kmod-libphy=y
          CONFIG_PACKAGE_kmod-mdio-gpio=y
          CONFIG_PACKAGE_kmod-switch-dev=y
          CONFIG_PACKAGE_kmod-mac80211=y
          CONFIG_PACKAGE_kmod-cfg80211=y
          CONFIG_PACKAGE_kmod-drm=y
          CONFIG_PACKAGE_kmod-drm-rockchip=y
          CONFIG_PACKAGE_kmod-mmc=y
          CONFIG_PACKAGE_kmod-sdhci=y
          CONFIG_PACKAGE_kmod-usb2=y
          CONFIG_PACKAGE_kmod-usb3=y
          CONFIG_PACKAGE_kmod-usb-storage=y

          # 完整固件
          CONFIG_TARGET_ROOTFS_SQUASHFS=y
          CONFIG_TARGET_ROOTFS_EXT4FS=n
          EOF
          make defconfig

      - name: Install jws3399.dtb
        run: |
          mkdir -p target/linux/rockchip/dts/rk3399
          cp jws3399.dtb target/linux/rockchip/dts/rk3399/

      - name: Download
        run: make -j4 download

      - name: Build
        run: make -j2 V=s

      - name: Upload firmware
        uses: actions/upload-artifact@v4
        with:
          name: openwrt-jws3399-final
          path: bin/targets/rockchip/armv8/
          retention-days: 30
